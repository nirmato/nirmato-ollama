name: "CodeQL Analysis"

on:
    workflow_dispatch:
    pull_request:
        types:
            - reopened
            - synchronize
            - opened
        branches:
            - "main"
        paths-ignore:
            - "**.md"
            - ".idea/**"
            - ".editorconfig"
            - ".gitignore"

jobs:
    analyze:
        uses: ./.github/workflows/codeql_kotlin.yml
        secrets: inherit
